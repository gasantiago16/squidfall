# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts

# Django
*.sqlite3

# Secrets — never commit real keys (copy the .example and fill in)
tools/.env

# Root compose build/deploy config (mirror URLs etc.) — copy from .env.example
/.env

# OS
.DS_Store
Thumbs.db
